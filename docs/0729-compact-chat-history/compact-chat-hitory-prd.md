# 需求
1. 需要在会话压缩完上下文后能够在会话页面能够展示完整的历史消息 和 压缩的提示信息，以压缩的提示信息为界。
2. 上下文压缩后只是在会话页面前端能正常展示 完整的会话记录（和压缩前一致）。
3. 优先以默认的scroll压缩策略为主进行分析、设计、实现

# 现状
举例：会话 sessionid：1785340717611-ikrmq85、chatid：09b93e32-73ec-475c-ab72-07ccf1454da3
会话A压缩上下文（/compact）后切换会话再切回来，被压缩驱逐的旧会话消息无法在会话页面显示。已经压缩的部分如示例展示：
C:\Users\朱思清\.qwenpaw\workspaces\default\sessions\console\default_1785340717611-ikrmq85.json(也可以在数据库中查看)

# 约束
尽量新增代码/功能，避免修改引入问题或者影响现有功能，修改范围尽量小，1.请你先阅读关于上下文压缩、会话记录保存与展示的前后端代码前后端耦合尽量小

# 任务
三方件代码如agentscope等在 .\.venv\Lib\site-packages路径下，qwenpaw代码分支2.0.1，撰写的文档都要保存在 docs/0729-compact-chat-history下面
1. 请你先阅读关于上下文压缩（主要是默认的scroll策略以及其它）、会话记录保存与展示的前后端代码，梳理代码逻辑、数据流转，撰写文档保存在docs/0729-compact-chat-history下面
2. 了解完现有代码后，请你仔细阅读需求内容，使用SDD范式，根据需求内容完成以下相应的文档（标上序号）写入docs/0729-compact-chat-history
需求与功能分析（明确需求内容、功能与边界）、
需求方案分析与设计（整体架构、数据流转、细节方案）、
开发方案（说明你打算使用哪些设计模式（例如策略模式、工厂模式等），并列出类名、接口以及它们之间的关联）与测试验证方案。
每完成一个方案都需要回头审视是否会影响现有功能、是否会修改引入问题，并进行优化。
3. 方案都完成后使用tdd进行开发